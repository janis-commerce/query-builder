{
  "name": "@janiscommerce/query-builder",
  "version": "1.3.6",
  "description": "Prepare and execute SELECT, INSERT, UPDATE, REMOVE queries from SQL database.",
  "main": "index.js",
  "scripts": {
    "lint": "eslint index.js lib/ tests/",
    "test": "export TEST_ENV=true; mocha --exit -R nyan --recursive tests/",
    "watch-test": "export TEST_ENV=true; mocha --exit -R nyan -w --recursive tests/",
    "test-ci": "nyc --reporter=html --reporter=text mocha --recursive tests/",
    "coverage": "nyc npm test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/janis-commerce/query-builder.git"
  },
  "keywords": [],
  "author": "Janis",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/janis-commerce/query-builder/issues"
  },
  "homepage": "https://github.com/janis-commerce/query-builder#readme",
  "dependencies": {
    "@janiscommerce/logger": "^1.0.2"
  },
  "devDependencies": {
    "eslint": "^7.13.0",
    "eslint-config-airbnb-base": "^13.2.0",
    "eslint-plugin-import": "^2.22.1",
    "husky": "^4.3.0",
    "mocha": "^8.2.1",
    "nyc": "^15.1.0",
    "sinon": "^9.2.1"
  },
  "files": [
    "lib/"
  ]
}
